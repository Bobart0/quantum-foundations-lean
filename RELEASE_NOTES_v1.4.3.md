# Release notes — v1.4.3

Journal-neutral publication snapshot.

No pre-existing theorem body is changed relative to the preceding publication
snapshot. The direct Gleason dependency is repinned to neutral release
`v1.1.2`, whose Lean sources are byte-identical to the scientific snapshot
previously consumed here.

Neutral publication entry points are:
`PUBLICATION_ARTIFACT.md`,
`QuantumFoundations/Audit/PublicationCore.lean`, and
`scripts/verify_publication.sh`. Historical journal-specific filenames remain
only as compatibility wrappers.
